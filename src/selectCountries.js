// will work on static covid results later

/* import React, { Fragment, useState, useEffect } from "react";
import "./App.css";

const byCountryUrl = "https://coronavirus-19-api.herokuapp.com/countries/";
const ja = "Jamaica";
const gh = "Ghana";
const us = "USA";

const SelectCountries = () => {

  return (
    <Fragment>
      <section id="country">
        {console.log(data.usa)}
        { {data.usa.country +
          ": }" +
          "Total: " +
          data.usa.cases +
          " | " +
          "Deaths: " +
          data.usa.deaths +
          " | " +
          "New: " +
          data.usa.todayCases} }
      </section>
      <section id="country2"></section>
    </Fragment>
  );
};

export default SelectCountries;
 */
